{
  "name": "hackathon-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Hackathon project submission, team matching and judging platform",
  "scripts": {
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "seed": "npm --prefix backend run seed",
    "dev": "concurrently -n backend,frontend -c blue,green \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "start": "concurrently -n backend,frontend -c blue,green \"npm --prefix backend start\" \"npm --prefix frontend run preview\"",
    "test": "npm --prefix backend test",
    "seed:users": "node scripts/seed-users.mjs",
    "seed:projects": "node scripts/seed-projects.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
